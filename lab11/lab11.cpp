/*
    Start program
    Program randomly selects any number between 0 and 21 and stores the number
    User guesses the number the program has picked and presses "Enter" key to send to program
    Program reads input from user and compares it to the number it randomly selected
    If user input is correct number, they win
    If user input is too low, program will tell the user to guess "higher" 
    If user input is too high, program will tell user to guess "lower"
    Program will let user guess until they get the right number
    User has 10 points
    Program keeps score for user
    Program will subtract 1 point for each incorrect guess
*/